version: 0.0
os: linux

files:
  - source: /
    destination: /home/ec2-user/calculator

permissions:
  - object: /
    pattern: "**"
    owner: ec2-user
    group: ec2-user
hooks:
  ApplicationStart:
    - location: 
      runas: ec2-user
